# OpenTTD & LLM

Use an LLM to drive an OpenTTD AI. 

## Getting started

1.  Install dependencies with `uv sync`
2.  Startup OpenTTD and the LLM interface: `uv run main.py`
3.  Start the AI through the in-game console with `start_ai OpenTTDLLM`

## Architecture

```mermaid

```